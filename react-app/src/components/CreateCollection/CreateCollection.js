import CollectionForm from '../CollectionForm';

import './CreateCollection.css';

const CreateCollection = () => {
    return (
        <div className="create-collection-container">
            This is the create collection page.
            <CollectionForm/>
        </div>
    );
};

export default CreateCollection;